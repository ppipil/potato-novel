## 1. Frontend Style Tag UX

- [ ] 1.1 将自由创作区的风格控制改为单选标签，固定支持 `言情`、`悬疑`、`恐怖`、`搞笑`
- [ ] 1.2 保留一个可选补充说明输入，并明确其语义为“补充要求”而不是主风格
- [ ] 1.3 调整前端请求负载与本地状态结构，确保 `styleTag` 成为稳定字段

## 2. Generation Pipeline Style Enforcement

- [ ] 2.1 在后端为四个标签建立明确的风格画像与骨架映射
- [ ] 2.2 让 `styleTag` 显式进入自定义故事骨架选择、节点正文 prompt 和节点选项 prompt
- [ ] 2.3 降低补充说明字段权重，使其只承担微调职责
- [ ] 2.4 为生成失败日志补充 `styleTag` 相关上下文，便于排查某个标签下的生成问题

## 3. Backend Module Organization

- [ ] 3.1 盘点当前故事生成相关的根模块，并确认各自归位目录
- [ ] 3.2 迁移 `story_prompts.py`、`model_providers.py`、`openings.py` 到目标分层目录，更新引用
- [ ] 3.3 按职责迁移或拆分 `story_text.py`，避免继续作为根目录杂糅工具模块存在
- [ ] 3.4 迁移 `integration.py` 到更合适的应用级目录，并保持现有接口行为不变
- [ ] 3.5 清理本次触及模块的头部说明和中文 docstring，确保 `main.py` 不因新功能继续变长

## 4. Validation

- [ ] 4.1 运行前端构建，确认风格标签入口和请求结构可正常通过构建
- [ ] 4.2 运行后端编译或等价校验，确认模块迁移后的 import 全部可用
- [ ] 4.3 至少手动验证两种风格标签的生成结果差异明显，不是同一故事换少量措辞
